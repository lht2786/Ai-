# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 项目性质

这是一个**中文玄幻网络小说的连载写作项目**，不是软件代码库。没有构建、测试、lint 命令，唯一的工具是 Git（用于给稿件做版本管理）。不要浪费时间寻找 `package.json`、源码目录或可执行文件——都不存在。

用户是非技术中文用户，全程用中文自然语言交流，不熟悉终端命令。

## 文件结构

- `我的小说（润色版）.md` —— **唯一的正文文件**，目前 36 章。第 1–27 章已润色；文件中段有一条分隔线和"以下为原稿，尚未润色"的说明，其后的第 28–36 章仍是原稿。每润色完一章，就把那一章移到分隔线之上，分隔线随之下移。
- `小说大纲.md` —— 大纲 / 设定源。含：一句话简介、核心设定（世界、修炼体系、金手指、功法）、人物表、分卷章节大纲、未解决伏笔、润色注意事项、整体节奏。

> 早先还有一个 `我的小说.md`（36 章原稿），内容已并入润色版，文件已于 2026-08-21 删除。需要旧版本时从 Git 历史里取。

**正文与大纲必须保持同步**：新增、重写、润色或删除章节时，除了改正文，还要同步更新大纲——章节目录行、大纲开头的润色进度说明、必要时补人物表或伏笔。大纲是剧情的"真相源"，正文服从大纲，大纲随正文演进。

**正文开头的人物表 / 世界观，与大纲里的对应小节也要保持一致**：两处都写了同样的设定，改一处就得改另一处。

## 章节格式约定

- 章节标题：`### 第X章 标题`，用中文数字（如 `### 第三十五章 幕后`）。
- 每章正文末尾以 `（第X章 完 · 待续）` 收尾。
- 正文文件开头有 `简介` / `主要人物` / `世界观` 三个小节，均已填写，改动设定时要一并维护。

## 写作风格（沿用已有正文）

- 网络小说白话文，第三人称，爽文节奏。
- 系统提示音用方括号：`【检测到宿主灵魂波动……】`。
- 全角标点、中文引号（`"…"`）。**原稿段（第 26 章以后）的标点虽已机械全角化，遣词和描写密度仍是原稿水准**，润色时按润色段的标准重写。
- 每章篇幅约 40–80 行（约 1500–3000 字），可参考相邻章节。已润色的章节明显长于原稿章节，以润色段为准。
- 招式命名有分工：《裂山拳》七式用双字（破岳、崩云、压顶、撼海、裂石、贯日、摩天），《碎石拳》三式用单字（崩、裂），不要混用。

## 工作流

1. 用户用中文提出要求（"续写第 X 章""润色第 X 到 Y 章""新增人物"等）。**当前主线工作是逐章润色第 28–36 章。**
2. 直接编辑 `.md` 文件，无任何编译或验证步骤。
3. 改完正文后同步更新 `小说大纲.md`。
4. 提交信息用中文描述改动（如"新增第三十五章：…"）。**除非用户要求，不要主动提交**；要提交时仅提交本次相关文件。
5. 用户说"上传"，指的是提交并推送：`git push origin main`（origin 是码云 gitee，可用）。另有一个 `github` 远程，网络连不上，不用管它。
