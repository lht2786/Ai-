# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 项目性质

这是一个**中文玄幻网络小说的连载写作项目**，不是软件代码库。没有构建、测试、lint 命令，唯一的工具是 Git（用于给稿件做版本管理）。不要浪费时间寻找 `package.json`、源码目录或可执行文件——都不存在。

用户是非技术中文用户，全程用中文自然语言交流，不熟悉终端命令。

## 文件结构

- `我的小说.md` —— 小说正文，目前 35 章。这是对外呈现的"成品"。
- `小说大纲.md` —— 大纲 / 设定源。含：一句话简介、核心设定（世界、修炼体系、金手指）、人物表、分卷章节大纲、未解决伏笔、整体节奏。
- `.claude/想法` —— （当前为空的）想法 / 笔记草稿文件。

**正文与大纲必须保持同步**：新增、重写或删除章节时，除了改正文，还要同步更新大纲——新增章节目录行、更新大纲开头的"截至第 X 章"、必要时补人物表或伏笔。大纲是剧情的"真相源"，正文服从大纲，大纲随正文演进。

## 章节格式约定

- 章节标题：`### 第X章 标题`，用中文数字（如 `### 第三十五章 幕后`）。
- 每章正文末尾以 `（第X章 完 · 待续）` 收尾。
- 正文文件开头有 `简介` / `主要人物` / `世界观` 三个占位小节（仍是空模板，未填写）。

## 写作风格（沿用已有正文）

- 网络小说白话文，第三人称，爽文节奏。
- 系统提示音用方括号：`【检测到宿主灵魂波动……】`。
- 全角标点、中文引号（`"…"`）。
- 每章篇幅约 40–80 行（约 1500–3000 字），可参考相邻章节。

## 工作流

1. 用户用中文提出要求（"续写第 X 章""重写第 X 到 Y 章""新增人物"等）。
2. 直接编辑 `.md` 文件，无任何编译或验证步骤。
3. 改完正文后同步更新 `小说大纲.md`。
4. 提交信息用中文描述改动（如"新增第三十五章：…"）。**除非用户要求，不要主动提交**；要提交时仅提交本次相关文件。
